<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="willard.cis46300.pridepass">

    <uses-permission android:name="android.permssion.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <activity android:name=".DriverPassenger">
            </activity>
        </activity>
        <activity android:name=".DriverDashboard">
        </activity>
        <activity android:name=".ClientDashboard">
        </activity>


    </application>

</manifest>
